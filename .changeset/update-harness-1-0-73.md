---
'@e2b/ai-sdk-sandbox': patch
---

Update to @ai-sdk/harness 1.0.73: implement the new `getPortEndpoint()` on the network sandbox session (`getPortUrl()` is deprecated upstream but kept for compatibility), and bump e2b to 2.39.0.
